#include <iostream>
//This is for a list
 using namespace std;

struct  Node{
    int data;
    Node* link;
};

int main() {
    /*
     * Beginning of linked lists
    Node* A;//Emptylist
    A = nullptr;//Start of the linked list

    //Create a node
    //we need to return the location of memory because malloc returns void
    Node* temp =  (Node*)malloc(sizeof(Node));//Memory reference

    temp->data = 2;
    //Creating a list dynamically
    Node* temp1 = A;

    //TRaversal of the list until the end of list
    while(temp1->link != nullptr){
        temp1 = temp1->link;
    }
    temp1->link = temp;
    temp->link = temp;
    A =  temp;

    temp = new Node();
    temp->data = 4;
    temp->link = nullptr;


    return 0;
     */
    /*
     * LInked list at the beginning of a linked list
     */
}